# content — 題庫與標準解答（真相層）

每題一個 markdown（進 git），sync script upsert 進 PostgreSQL。
**合規：只收官方公開考題，每檔 frontmatter 必含 `source_url` 與 `license_status`。**
檔案格式與同步流程見 [docs/03-content-pipeline.md](../docs/03-content-pipeline.md)。
