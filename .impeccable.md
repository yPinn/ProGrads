# Design Context — ProGrads

## Target audience

台灣研究所考生(備考者)。多為大學高年級／應屆／重考生,需在報名季密集查詢校系招生資料、練歷屆考古題、追蹤招生時程。

## Use cases

裝置策略(非 mobile-first):

- **Desktop = 查詢效率**:跨校系比對招生組別、名額/錄取率、採計考科佔分;考古題依考科/學校/年度/題型檢索。
- **Phone = 快速查詢/確認**:臨時查某校系時程、某張卷題號。
- **Tablet = 閱讀/練題**:題幹、選項、解析的長文閱讀與題號導覽。

## Brand personality / tone

**Editorial Minimal** —— 沉穩、可信賴、低干擾、資訊密度高。

- **字**:標題與長文閱讀用明體(自託管 GenWanMin2 TW)建立識別;UI chrome／表格用 Inter + Noto Sans TC;程式碼 Fira Code。
- **色**:`violet` 點綴 + `zinc` 近黑中性(Nuxt UI semantic),非霓虹、非深色發光風。
- **形**:銳利圓角(0.375rem)、左對齊、虛線 empty state、uppercase eyebrow + tracking。
- **動**:低干擾,僅 `transition-colors` 級別的狀態回饋,不做彈跳/花俏動效。

## Tech constraints

Nuxt 4 + Nuxt UI v4 + Tailwind v4(`@theme` 語意 token)+ MDC + TanStack Query;Cloudflare Pages(內容頁 prerender、API 驅動頁 client-side render);KaTeX 數學式;dark mode 經 Nuxt UI + ClientOnly 防 hydration 位移。

## Token system

三層:primitive(Tailwind 預設)→ semantic(`@theme` 的 `--text-*`/`--spacing-*`/`--leading-*`…)→ component(`:root` 的 `--bottom-bar-height`/`--safe-*` 等)。iOS 基礎:`--spacing-touch`(44px)、`--safe-*`、`100dvh`。詳見 `tasks/todo.md`。
