# API runtime config.
PORT=8088
HOST=0.0.0.0
# Same PostgreSQL as packages/db (docker-compose host port 5433; bound to localhost).
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5433/prograds?schema=public"
