# Bind PostgreSQL to localhost only; never expose publicly.
# Matches the local docker-compose.yml service (host port 5433). Copy this file to .env.
DATABASE_URL="postgresql://prograds:prograds@localhost:5433/prograds?schema=public"
