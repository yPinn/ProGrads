# Same PostgreSQL as packages/db (docker-compose host port 5433). Copy to .env.
# In production the sync runs on the server where the DB is reachable on localhost.
DATABASE_URL="postgresql://prograds:prograds@localhost:5433/prograds?schema=public"
# Optional: override the content root (default: <repo>/content).
# CONTENT_DIR="../../content"
